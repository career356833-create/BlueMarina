import type {MarketAudit,MarketImageRecord,MarketRepository,MarketReview,StoredMarketListing} from "./types";
export class InMemoryMarketRepository implements MarketRepository{listings:StoredMarketListing[]=[];reviews:MarketReview[]=[];audits:MarketAudit[]=[];images:MarketImageRecord[]=[];async find(id:string){return this.listings.find(x=>x.listing.id===id)??null}async findByIdempotency(s:string,k:string){return this.listings.find(x=>x.listing.sellerId===s&&x.idempotencyKey===k)??null}async findByContentHash(s:string,h:string){return this.listings.find(x=>x.listing.sellerId===s&&x.contentHash===h)??null}async list(l:number){return this.listings.slice(0,l)}async listBySeller(s:string,l:number){return this.listings.filter(x=>x.listing.sellerId===s).slice(0,l)}async listPublic(l:number){return this.listings.filter(x=>x.listing.status==="ACTIVE").slice(0,l)}async create(v:StoredMarketListing){this.listings.push(structuredClone(v));return v}async update(v:StoredMarketListing){const i=this.listings.findIndex(x=>x.listing.id===v.listing.id);if(i<0)throw new Error("PERSISTENCE_FAILED");this.listings[i]=structuredClone(v);return v}async appendReview(v:MarketReview){this.reviews.push(structuredClone(v))}async appendAudit(v:MarketAudit){this.audits.push(structuredClone(v))}async countImages(id:string){return this.images.filter(x=>x.listingId===id).length}async listImages(id:string){return this.images.filter(x=>x.listingId===id).map(x=>structuredClone(x))}async appendImage(v:MarketImageRecord){this.images.push(structuredClone(v))}}
