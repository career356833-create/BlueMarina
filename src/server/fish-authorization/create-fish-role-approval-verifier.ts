import { FishRoleApprovalVerifierService } from "@/domain/fish-authorization/approval"; import type { FishRoleApprovalRepository } from "@/domain/fish-authorization/ports/fish-role-approval-repository"; import type { FishRoleApprovalAuditLog } from "@/domain/fish-authorization/ports/fish-role-approval-audit-log"; import type { FishRoleClock } from "@/domain/fish-authorization/ports/fish-role-clock"; import type { FishRoleApprovalServerConfig } from "./fish-role-approval-server-config";
export function createFishRoleApprovalVerifier(config: FishRoleApprovalServerConfig, repository: FishRoleApprovalRepository, audit: FishRoleApprovalAuditLog, clock: FishRoleClock) { return new FishRoleApprovalVerifierService(config.enabled, repository, audit, clock); }
