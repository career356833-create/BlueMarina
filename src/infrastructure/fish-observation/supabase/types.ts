export type FishSupabaseRow = Record<string, unknown>;
export interface FishSupabaseClient { select(table: string, query: Record<string, unknown>): Promise<FishSupabaseRow[]>; insert(table: string, row: FishSupabaseRow): Promise<FishSupabaseRow>; update(table: string, patch: FishSupabaseRow, match: FishSupabaseRow): Promise<FishSupabaseRow | null>; delete?(table: string, match: FishSupabaseRow): Promise<number>; }
export interface FishStorageTransport { createSignedUploadUrl(bucket: string, path: string, expiresIn: number): Promise<string>; createSignedUrl(bucket: string, path: string, expiresIn: number): Promise<string>; exists(bucket: string, path: string): Promise<boolean>; read(bucket: string, path: string): Promise<Buffer | null>; upload(bucket: string, path: string, data: Buffer, mimeType: string): Promise<void>; remove(bucket: string, paths: string[]): Promise<void>; }
