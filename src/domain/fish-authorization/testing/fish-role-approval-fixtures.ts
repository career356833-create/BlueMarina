import { createHash } from "node:crypto"; import type { FishRoleApprovalRecord } from "../approval/types";
export const approvalRecord = (reference = "APPROVAL-0001", changes: Partial<FishRoleApprovalRecord> = {}): FishRoleApprovalRecord => ({ approvalId: "approval-1", referenceHash: createHash("sha256").update(reference).digest("hex"), action: "grant", targetUserId: "target", requestedRole: "fish_reviewer", targetIdentityType: "human_user", requestedBy: "admin", approvedBy: "approver", approvedAt: "2026-08-04T00:00:00.000Z", expiresAt: "2026-08-05T00:00:00.000Z", status: "approved", scope: "fish_role", version: 1, ...changes });
