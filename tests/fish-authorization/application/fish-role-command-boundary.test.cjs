const assert = require("node:assert/strict"); const fs = require("node:fs"); const path = require("node:path"); const test = require("node:test");
const root = path.resolve(__dirname, "../../..");
test("server boundary is configuration-driven, route-free, and does not embed a Supabase client", () => { const config = fs.readFileSync(path.join(root, "src/server/fish-authorization/fish-role-server-config.ts"), "utf8"); const server = fs.readFileSync(path.join(root, "src/server/fish-authorization/create-fish-role-management-service.ts"), "utf8"); assert.match(config, /FISH_ROLE_MANAGEMENT_ENABLED/); assert.match(config, /=== "true"/); assert.doesNotMatch(server, /supabase|service[_ -]?role/i); assert.equal(fs.existsSync(path.join(root, "src/app/api/fish-authorization")), false); });
