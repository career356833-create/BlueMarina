const assert=require("node:assert/strict"),test=require("node:test"),fs=require("node:fs"),path=require("node:path"); const root=path.resolve(__dirname,"../../.."); test("storage adapter keeps URLs transient and rejects traversal",()=>{const source=fs.readFileSync(path.join(root,"src/infrastructure/fish-observation/supabase/supabase-fish-storage-provider.ts"),"utf8"); assert.match(source,/includes\("\.\."\)/); assert.match(source,/SUPABASE_SIGNED_UPLOAD_PROVIDER_TTL_SECONDS = 7200/);});
