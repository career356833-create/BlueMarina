const assert=require("node:assert/strict"),test=require("node:test"),fs=require("node:fs"),path=require("node:path"); const root=path.resolve(__dirname,"../../.."); test("adapter source maps snake case and declares CAS",()=>{const source=fs.readFileSync(path.join(root,"src/infrastructure/fish-observation/supabase/supabase-fish-media-repository.ts"),"utf8"); assert.match(source,/expectedVersion/); assert.match(source,/mediaFromRow/);});
