const assert = require("node:assert/strict"); const test = require("node:test"); const { createFixture } = require("./test-helpers.cjs");
test("marks deletion pending, revokes public output, and queues cleanup", async () => { const f = createFixture(); const created = await f.service.createObservationUpload({ actorUserId: "user-1", actorRole: "user", observationId: "obs-1", expectedMimeType: "image/jpeg", expectedByteSize: 1, purpose: "user_original_upload", idempotencyKey: "create" }); const result = await f.service.requestMediaDeletion({ actorUserId: "user-1", actorRole: "user", observationId: "obs-1", mediaId: created.mediaId, purpose: "delete_media", idempotencyKey: "delete" }); assert.equal(result.state, "delete_pending"); assert.equal(f.cleanupQueue.jobs.length, 1); assert.equal(f.storage.revoked.length, 1); });
