const assert=require("node:assert/strict"),test=require("node:test"),fs=require("node:fs"),path=require("node:path");const root=path.resolve(__dirname,"../../..");test("gateway defaults disabled and does not include credentials",()=>{const s=fs.readFileSync(path.join(root,"src/server/fish-observation/media/fish-media-server-config.ts"),"utf8");assert.match(s,/FISH_MEDIA_GATEWAY_ENABLED/);assert.doesNotMatch(s,/SUPABASE.*KEY/);});
